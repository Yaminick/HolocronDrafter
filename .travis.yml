language: csharp
solution: IsochronDrafter.sln
sudo: false

install:
  - pip install --user coveralls pytest pytest-cov pyopenssl ndg-httpsclient pyasn1

script:
  - py.test
  
after_script:
  - coveralls

